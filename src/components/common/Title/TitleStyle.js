import { StyleSheet } from 'react-native';

export const TitleStyle = StyleSheet.create({
  view: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    margin: 10,
    paddingLeft: 10,
  },
  text: {
    fontSize: 25,
    fontWeight: 'bold',
  },
});
